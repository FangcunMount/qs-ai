# M5 服务端收尾记录

2026-09-19 核对：旧实现代码已退出生产，定向清库已经完成。数据库结构退役迁移 QS #111 已合并并部署，Mongo 35 为 clean，启动后九个旧集合仍不存在。服务端退役与清库已完成。微信正式版本由用户在清库后发布，不再作为服务端清库的前置条件；正式发布和真机复验仍单独保留为未完成项。

本页是当前状态，其他迁移文档的带日期记录保留历史语境。不能用 CI 成功替代实际镜像、迁移账本或真实业务证据。

## 实际删除与保护结果

2026-09-15 01:36:31–01:59:48 UTC 的维护窗口内，停止 QS API、collection、Worker，以及 AI API、gRPC、评测、执行、投递进程；入口维护状态覆盖写入。完成同一清单的备份、隔离恢复与摘要核对后，01:56:44 UTC 执行完成：

- 固定九个旧 AI Mongo 集合，共 59 条记录，已删除。
- 共享 Mongo outbox 中固定六类旧 AI 事件，共 1,145 条，已定向删除；共享集合与其他事件保留。
- QS 和 qs-ai 两个 MySQL 库没有满足删除条件的记录，本轮没有 SQL 数据删除。QS 的 214,005 条共享死信、三个桥接表及 AI 全部新会话、任务、租约、资产和发布证据保留。
- QS MySQL 70 个表以数量、结构摘要、原生扩展校验和及外键清单复核，桥接表另计算完整行 SHA-256；AI 和 Mongo 的受保护数据采用完整摘要。维护窗口前后 SQL 校验文件逐字节一致，不将原生校验和描述为逐行密码学摘要。
- 原 NSQ 失败交接 Channel 中 16 条旧事件先归档并逐字节恢复验证，再确认；共享 Topic 未清空。两个旧 AI Redis 锁不存在，没有 Redis 删除。
- 已暂停的 Mongo profiling 恢复原配置，服务恢复健康，入口退出本次维护状态。总维护时间约 23 分 23 秒。

9 月 19 日复查：九个集合仍不存在，旧 outbox 事件为 0，原业务及失败交接 Channel 的积压、在途和延迟数均为 0。备份与本地收据的九份 JSON 文件摘要一致。临时隔离 Mongo 恢复容器已停止，恢复工具、镜像与归档保留。

## 当前业务证据

当前 v6 指纹仍为 `sha256:cc747df0a6ae4b02b65b7447ce08845fa8c4e91b9d674f51ac25fbcbffd2a2a2`，发布指针版本 1，发布内容摘要校验通过。成功测评的持久状态为 completed/version 4，一次执行、一次模型调用，版本 2、3、4 的事件全部投递；原成果和固定发布配置保留。具体测试主体与请求标识存于私有验收归档，不在仓库复制个人信息。

此前已完成真实 SELF 授权关系的撤销、拒绝、原关系恢复与允许；实际第二账号访问拒绝；重复及乱序回执不覆盖终态；重启保持原配置和成果。隔离 MySQL 恢复测试 39 项覆盖真实子进程终止、持久回执恢复和未知调用不盲目重发。生产未做破坏性故障注入。用户已确认小程序开发/体验版本展示成功；这不等于微信正式发布。

## 版本与恢复边界

- QS 代码退役：[PR #110](https://github.com/FangcunMount/qs-server/pull/110)，删除时生产基线 `d44ab9f68e87ffccbf59d9461416603b86442be7`，现已由下述结构迁移版本替换。
- QS 结构退役：[PR #111](https://github.com/FangcunMount/qs-server/pull/111)，合并提交 `71f95fbae283291e383c23cb49aaa4430fe07983`；[生产部署成功](https://github.com/FangcunMount/qs-server/actions/runs/35432119141)。部署后账本为 35/clean，旧集合和旧 outbox 仍为 0，真实 mTLS 授权与跨主体拒绝、旧 RPC 不注册均复验通过。迁移 35 仅允许空或不存在的九个旧集合，非空明确拒绝。
- Operating：[PR #35](https://github.com/FangcunMount/qs-operating-system/pull/35)，已部署 `68b9a3cf7f4684ed02ac73c763284984fb4a1b15`。
- qs-ai 业务镜像仍为 `a670d89a6768e7e625e5dc4bcc98c618664c614a`，维护工具最终版本为 PR #94 合并 `06c612cf1716b0e74c7727b54801afdb39801ff5`。维护工具不触发应用部署，不虚构新运行镜像。

数据库备份在 serverA `/data/backups/qs-ai-m5/database-retirement-20260915T013614Z`，至少保留至 **2026-09-22 01:56:44 UTC（北京时间 09:56:44）**。清单 SHA-256 为 `cf86c43d749d58dab06abf4d84095634d2101fcc3f84952dad07ceddbe8c9832`，备份 SHA-256 为 `1ac3bedc73c5f820652c2c875496550b7f180f2fb995f3fdec4cdf7148647f7b`。目录内包含执行、恢复验证、SQL 保护、服务恢复收据及 `RECOVERY.md`。

NSQ 归档在 serverD `/data/backups/qs-ai-m5/nsq-inspection-20260915`，收据在同级 `nsq-retirement-20260915`，至少保留至 9 月 22 日 00:44:25 UTC。敏感正文不进入 Git/CI。备份到期不自动删除；保留期不阻塞验收。

删除后禁止直接回滚到依赖旧集合的镜像，迁移 down 不等于数据恢复。恢复需先用原工具在隔离库复核归档，再建立停写窗口按原主键与集合清单恢复；保留新数据并拒绝冲突。临时维护接线已移除，恢复工具在保留期内有明确用途，不属于运行时兼容层。
